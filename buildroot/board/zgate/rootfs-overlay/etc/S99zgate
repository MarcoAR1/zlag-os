#!/bin/sh
case "$1" in
  start)
    echo "Starting Z-Gate Agent..."
    # Ejecutamos en segundo plano y redirigimos salida a un log en RAM
    /usr/bin/z-gate-agent > /tmp/zgate.log 2>&1 &
    ;;
  stop)
    killall z-gate-agent
    ;;
  *)
    echo "Usage: $0 {start|stop}"
    exit 1
esac
exit 0